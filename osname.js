let osname = prompt("Enter your OS name :");
let version = prompt("Enter your version of the OS :");
console.log(`${osname} ${version}`);
console.log(`The OS name is ${osname} and the version is ${version}`);